name: three_sixty_media
description: "Flutter plugin to display 360° images (v0.1.0 Android core)."
version: 0.1.0
homepage:

environment:
  sdk: ^3.9.2
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.threesixtymedia.three_sixty_media
        pluginClass: ThreeSixtyMediaPlugin
